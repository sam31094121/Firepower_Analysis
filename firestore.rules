rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 暫時允許所有讀寫 (開發/測試用)
    // ⚠️ 上線前請修改為更嚴格的規則
    match /{document=**} {
      allow read, write: if true;
    }
    
    // 生產環境建議規則 (需啟用 Firebase Authentication):
    // match /records/{recordId} {
    //   allow read: if true;  // 所有人可讀
    //   allow write: if request.auth != null;  // 僅登入使用者可寫
    // }
    // 
    // match /employeeProfiles/{profileId} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
    // 
    // match /employeeDailyRecords/{recordId} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
  }
}
